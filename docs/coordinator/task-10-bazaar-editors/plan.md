# Task 10 — Bazaar editor consolidation

Implement [Task 10](../../performance-improvement-plan/tasks/10-bazaar-editor-consolidation.md) in the user-selected permanent worktree `C:\Users\Marco\OneDrive\git_projects\fnd-devs`, branch `devs`, starting at clean HEAD `6f080dbd9bd3f6380242fa0ea66894620d9ad958`. No applicable AGENTS.md was found. Existing Tasks 04/07/09 repositories and trusted writes are accepted inputs; Task 09's outstanding performance gates are not claimed closed here.

## Authority and scope

The user authorized Step 10 implementation, local tests/builds and deployment/interaction on `https://fatin-test.web.app`, then explicitly requested a PR into `main` after accepting the manual retest. The coordinator owns staging release, browser actions and the commit/push/PR from permanent `devs`. Developers may edit their scoped local code/tests and run assigned local verification; no external actions, nested agents, Git mutations, dependency changes or coordinator-file edits. Merge and production release are outside this request. Preserve existing schemas, gameplay, create/edit/inventory modes, media durability and lazy editor boundaries.

Only plan.md and control.md are coordinator artifacts. Developer results return in messages. Use Astra Light (`gpt-6-astra / low`) for each new unit; after two confirmed implementation failures escalate to Astra High (`gpt-6-astra / high`), stopping after two High failures. Failure counts persist per unit.

## Schedule and gates

| Stage/mode | Outcome and dependency | Owned writes/resources | Exit gate |
|---|---|---|---|
| A / sequential | 10A: measure and establish parity, then shared on-demand directory/config ownership; consumes current Tasks 04/07/09 | Four Bazaar editor adapters, small editor data helpers/tests, directly necessary config/directory integration tests; local test resource | Coordinator source review; four-type create/edit/inventory data parity; explicit before/after read counts; complete paged directory only on intent; zero local-edit reads; no direct common collection listeners |
| B / sequential | 10B: structural form updates, measured memoized sections and bounded media flow; requires 10A | Same editors and editor form helpers/tests; narrowly required existing Task 07 media helpers/tests; local test/build resource | Coordinator review; typing render counts improve; parity green; upload cap/failure/metadata/cleanup/cancel gates; player bundle excludes editor implementations |
| C / coordinator gate | Combined verification and staging release; requires A+B | Tests/build/guarded staging deployment, fatin-test browser session | Relevant full automated gates pass; ordinary staging artifact verified; deployed identity confirmed; actual editor interactions observed and temporary test changes reconciled |
| D / coordinator gate | User-requested persisted manual browser scenarios on staging | Authenticated fatin-test browser; clearly named QA catalog/inventory fixtures only | Create/save/reload, spell/draft/visibility retention, Cancel, image replacement/removal and inventory-copy isolation verified; failures explicitly reported |
| E / coordinator gate | User-requested PR from devs into main; requires A–D | Scoped Git commit/push and GitHub PR | Reviewed diff committed/pushed, PR base/head verified and attached to this task |
| F / coordinator gate | Independent review and update of PR #42; reopen affected prerequisites before acceptance | Complete PR source review, GitHub discussions, focused/full checks and scoped commit/push/PR updates | Valid findings fixed and regression-tested, discussions addressed, same PR updated; no merge or production release |

2026-09-20 repair authorization: user requests fixing the manual findings, redeploying and repeating the same six scenarios. Reopen B/10B before repeating C and D. Attempt 2's live image-replacement defect consumes its second cumulative Light failure (the two browser reproductions count once); preserve attempt 1's failure. Escalate this existing workstream to Astra High, attempt 3. Add narrowly necessary Bazaar parent/editor lifecycle and catalog/media helper tests to 10B ownership; preserve security/account scope fences and accepted 10A behavior. Coordinator continues to own deployment and live testing. No backend/schema/dependency change without scope reconciliation.

Serialize because both units change the same adapters and share build output. No new worktree is needed.

2026-09-21 PR review authorization: review the complete diff and existing discussions, fix valid findings, commit/push to permanent `devs` and update PR #42 into `main`; keep the main checkout unchanged. Fresh head `31f0fd1df803bc64e4d896f037517b0e977a6ad4`, base `6f080dbd9bd3f6380242fa0ea66894620d9ad958`, clean worktree, hosted checks passed. Configuration retry retains a cached missing schema; reopen A/10A, count this once against accepted attempt 2 (Light now 2/2), and escalate to High attempt 3. Preserve prior B–E evidence; revalidate affected behavior after repair. Coordinator owns all external mutations and plan/control; developers own only assigned local repair/tests. Prior staging/manual results refer to Hosting version `66b4a5ede1c2f830`, not future review fixes.

## Measurement and release contract

Before product changes, run repeatable four-type fixtures on this machine, distinguish effect/repository calls from physical reads, and return measured cold/reopen/type-switch/local-edit values. Capture baseline typing section commits before 10B. Record observed values here; never infer them from source. Existing performance budgets remain in force. Additional numeric targets: 0 directory calls before custom visibility intent; every directory page resolved after intent, with 0 extra physical reads on cached reopen; common config load once per session with 0 physical reads from local field/custom-spell changes; 0 unrelated field-section commits for a field edit; at most 2 concurrent independent media uploads; no old media deletion before durable metadata.

Use the same fixture and measurement method before/after, including create/edit/inventory variants; tests count work and do not assert wall-clock timing. Normal player Bazaar must retain four dynamic editor imports and contain no editor implementation in its route chunk. No reader/writer migration is planned. Rollback is the previous staging Hosting release; preserve compatible backend contracts and avoid unrelated backend deployments. Staging observations and deterministic local metrics are separate evidence.

Staging preflight on 2026-09-20 passed the guarded read-only App Check and user-directory prerequisites (12/12 unchanged directory projections, zero writes). Previous Hosting version: `projects/fatin-test/sites/fatin-test/versions/5e8efc308c3c522f`, live release `1788682540008000` from 2026-09-06. Retain as rollback reference.

## Acceptance

Before measurements (original product source, Node 22.22.2/Jest jsdom on this Windows machine, `TASK10_FIXTURE_V1` in `editorDataParity.test.js`; 5 tests passed in 6.536s): 12 mounted cases cover four adapters x create/edit/inventory, nested Forza=17, custom/linked spells and an unknown selected UID. Cold Weapon/Armor repository calls: schema 3, common spells 2, techniques 2, directory 1; Accessory/Consumable: 2/1/1/1. Instrumented transport: 4 getDoc + 1 getDocs each cold. Local custom spell addition: +1 schema/spells/techniques call, 0 transport reads; typing: 0 calls/reads but 1 unrelated visibility and spell-button section commit each. Edit/inventory reopen calls 3/2/2/1 with 0 transport reads. Type sequence Weapon→Armor→Accessory→Consumable→Weapon: document reads 4/1/1/1/0, directory queries 1/0/0/0/0. Accessory/Consumable each establish 2 collection listeners per mount; listener-delivery reads were not simulated and are not claimed measured. Targets remain distinct above.

10A accepted initially: four adapters + editorData helpers/tests, config invalidation subscription and obsolete query-contract removal; /root/task10a attempt 2, t10a-20260920-02, Astra Light. Focused 9 suites/70 tests and 6 Node contracts passed; coordinator independently reproduced 44 data/repository tests and the four draft-refresh regressions now green. Eager directory calls 1→0; local custom-spell repository calls 1/1/1→0/0/0 (physical reads remain 0); Accessory/Consumable listeners 2→0; 200 users resolved through five cached pages. Transport measurements use real caches with mocked Firestore, not live billing. Reaccepted after PR review: /root/task10a_review_high attempt 3 / t10a-20260921-03 / Astra High invalidates item/spell schemas before retry, retaining common caches and initialized drafts. Six real-cache recovery cases demonstrated RED/GREEN, focused 4 suites/57 tests passed and coordinator independently ran the full 169 suites/1,633 tests. Cumulative Light failures 2/2 (attempt 1 draft reset, attempt 2 cached-null retry), last counted attempt 2/t10a-20260920-02; High 0/2. Developer and tests quiescent.

10B accepted after repair: /root/task10b_high attempt 3, t10b-20260920-03, Astra High. Earlier structural path updates and memoized parameter/visibility/spell presentation preserve explicit adapters. Across the same 12 cases, typing unrelated visibility/spell commits 1/1→0/0; all three parameter sections stay at 0. Base edits commit Base once and Combattimento/Special zero times. All local text/parameter/custom-spell changes produce zero repository/transport work. Canonical and legacy reduction fixtures preserve type differences and unknown options; explicit image retirement follows durable metadata. The repair routes the comparison panel's Edit action into Bazaar's existing stable, account-scoped editor snapshot and removes duplicate editor ownership. No adapter, media, auth, backend or schema change was needed for this repair. Coordinator source review found no blocker. Cumulative Light failures 2/2 (attempt 1 stale directory-boundary assertion; attempt 2 live image replacement); High failures 0/2. Last counted failure: attempt 2/t10b-20260920-02. All developers/processes quiescent.

10B media decision: reuse the existing Task 07 global upload queue (cap 1, within the numeric maximum 2) and durable cleanup/retry contracts; increasing its application-wide concurrency is not required. Verify editor integration and move explicit image retirement after successful catalog metadata save. Preserve necessary parent preparation for trusted media uploads; attachment metadata must follow successful upload/processing.

10B parity decision: realistic saved-item fixtures exposed Accessory/Consumable reading reductions from root while saving under General. Fix initialization to prefer canonical General reductions with legacy-root fallback; test each format separately. This is scoped schema preservation, not a migration or an implementation failure.

Repair evidence: the first persisted browser run passed scenarios 1–4 and 6 but exposed image replacement failure through the comparison-panel pencil. Selecting orange QA B then saving closed the editor and logged `Task07MediaPipelineError: Task 07 media owner was unmounted` at 14:03:43 and 14:07:23 UTC; reload still showed blue QA A. Media preparation advances catalog revision, temporarily masking useBazaarDetail's item and unmounting the panel-owned editor. All four new real-parent lifecycle tests reproduced this failure before the repair. The expanded 36-case regression suite exercises real Bazaar/detail/comparison/editor/media ownership, metadata/upload failure and retry, completed-root upload reuse, account/access/role/unmount cancellation and fresh selection after Cancel. Transports are mocked; this evidence is distinct from live browser testing.

Fresh final automated gates passed: focused 24 React suites/226 tests; full 169 suites/1631 tests (161.273s); coordinator independent lifecycle rerun 36/36; perf:test 491/491; release:test; bundle contracts 2/2; query/config/user/media boundaries and diff-check. The actual normal-staging module graph excludes all four adapters and editorForm from ordinary player groups, and all 63 JS asset names match the released build. No profiling or performance instrumentation is present. No product changes followed this verification. Sandbox-only process/config restrictions were resolved with required permissions; owned temporary processes/state were cleaned up.

Guarded `npm.cmd run fb:deploy:staging:hosting` from permanent devs completed successfully on 2026-09-20, rebuilding the ordinary artifact with App Check and 12/12 unchanged directory projections (zero writes). Existing Firebase authentication worked with Node's system certificate store (`NODE_OPTIONS=--use-system-ca`); the initial CLI auth failure caused no release and the retry succeeded. Live version `projects/fatin-test/sites/fatin-test/versions/66b4a5ede1c2f830`, release `1789916321303000`, released 2026-09-20T14:58:41.303Z. Served `main.f6c353d9.js` is 669447 bytes and SHA-256 `dca00dc3edd8e3ae8379d7552444e6a7b995230ae74fbd6730b531afb9f5f93c`, exactly matching the local build; Bazaar chunk is `route-bazaar.321c7911.chunk.js`. Signed-in IAB tab 1 reloaded this artifact. Immediate rollback reference is the preceding version `f43de4a33ea2dbe2`, release `1789910125464000` (13:15:25.464Z), main.8c8e406f.js.

Repeated manual browser acceptance on that release used four fresh catalog fixtures named `QA10 20260920 R2 Arma/Armatura/Accessorio/Consumabile`. All saves were allowed to finish before full reload/reopening through the panel pencil; no direct database writes or hidden application state were used.

| Requested scenario | Observed result |
|---|---|
| 1. Create/save all four types | Passed. Prices 201/202/203/204, effects, Forza 11–14/21–24/31–34/41–44 and Danno 1d6–4d6/1d8–4d8/1d10–4d10/1d12–4d12 persisted at levels 1/4/7/10. Hands=2, slotCintura=3, Cariche=4; consumable Bonus Creazione=5, duration=6, maxStack=7, belt slots=2, stackable=false, Effetto Temporaneo, environmental parameter Forza, Ingestione+Copertura Armi and Fuoco persisted. |
| 2. Add/edit spell without resetting draft | Passed for all four. Blue image selected before Add Spell; type-named QA10R custom spell cost edited 5→8. Name, parameters, image, Attacco reduction 2 and custom-spell reduction 3 survived nested editing, save and reload. Spell cost 8 also survived later item edits. |
| 3. Custom visibility | Passed for all four. Tutti→Utenti Selezionati, searched Marco, selected MarcoDM + MarcoTEST, searched/added/removed bro, cleared search and continued editing. Both intended users persisted after reload. |
| 4. Cancel and saved edits | Passed for all four. Temporary effect and Forza=999 canceled back to original values. Saved effects and Forza level1=61/71/81/91 persisted; untouched levels, type fields, spell cost, reductions, users and images remained. |
| 5. Replace/remove image | Passed. Original QA armor reproduced the old entry path successfully; all four R2 blue→orange replacements saved and visually displayed orange QA B after full reload. R2 weapon image removal then persisted. No fresh browser errors. |
| 6. Inventory-copy isolation | Passed. DM Dashboard granted two R2 weapon copies to MarcoTEST. First copy Forza 61→161 survived full reload; second copy and catalog remained 61. Other parameters, spell cost 8 and reductions remained, and gold stayed 3565. |

Optional live network interruption and duplicate-media retry were not manually simulated because the available browser has no offline control. Automated failure/retry tests passed; they are not reported as manual network evidence.

Staging data retained for review: the four R2 records (weapon without image, other three orange) and two R2 weapon copies in MarcoTEST. Earlier four QA records remain at prices 101/102/103/104; old weapon has no image, old armor is orange from the successful reproduction gate, old accessory/consumable are blue. Earlier two weapon copies in each of MarcoDM/MarcoTEST remain. No unrelated records or gold were changed. Browser is left on Bazaar filtered to `QA10 20260920 R2`, editor closed, marked deliverable. Publication uses permanent branch devs and implementation base HEAD above. Task 09's separate outstanding performance gates remain open.

E accepted: implementation commit `565628341b52d5a229b0439afddfa3c29fe57db8` pushed to `devs`; [PR #42](https://github.com/marco-lor/fnd/pull/42) is open, ready for review, targets `main` and is attached to the Codex task. GitHub reports mergeable; hosted CI is running and is not claimed passed. Publication changed only coordinator status and trailing blank lines in a test after the accepted local/staging gates; product code matches the tested release.

F review accepted (2026-09-21): independently reviewed the full PR diff, callers, GitHub discussion and passing intake CI at `31f0fd1`. The sole actionable finding was cached-null schema retry, repaired in 10A above; no additional blocker found across draft identity, account/access fencing, complete visibility paging, type/reduction preservation, media failure/retry/cleanup, inventory-copy isolation and lazy boundaries. Fresh coordinator gates: `CI=true npm.cmd test -- --watchAll=false --watchman=false --runInBand --silent` 169 suites/1,633 tests; `npm.cmd run perf:test` 491 tests; `npm.cmd run release:test` 62 tests; bundle contracts 2/2; query/config/user/media boundary commands; `npm.cmd run build:staging`, matching release-environment webpack stats, actual editor bundle boundary and disabled-instrumentation checks; all exit 0. Same-PR publication is authorized and owned by the coordinator; current GitHub CI/discussion state is authoritative there. Prior B–E evidence remains valid for unchanged behavior, but the retry repair was not redeployed or manually tested on staging. Main checkout, QA data, merge and production deployment remain untouched.
